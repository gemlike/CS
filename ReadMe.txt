donet new console
dotnet new sln (nếu dùng lệnh này để tạo thì phải move đến file chứa thư mục bin mới dotnet run được)

dotnet add package Newtonsoft.Json

Convert Json sang class C#:
https://json2csharp.com/s


dotnet add package System.Data.SqlClient